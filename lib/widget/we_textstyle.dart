import "package:flutter/material.dart";

import "we_size.dart";

/// 主要文本样式
TextStyle tsMain = TextStyle(fontSize: 15 * sp);

/// 主要值文本样式
TextStyle tsMainVal = TextStyle(
  fontSize: 15 * sp,
  color: Colors.grey[500],
);

/// 组文本样式
TextStyle tsGroupTag = TextStyle(
  fontSize: 12 * sp,
  color: Colors.grey[700],
);
